{
  "name": "vite-screen-record",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "npm run build && vite preview"
  },
  "devDependencies": {
    "vite": "^4.3.2",
    "vite-plugin-cross-origin-isolation": "^0.1.6"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.11.0",
    "@ffmpeg/ffmpeg": "^0.11.6"
  }
}
